export const sendTestMessage = async (token: string, chatId: string, url: string) => {
    return { success: true };
};

export const sendSignalToTelegram = async (signal: any, chatId: string, token: string, action: string, reason: string, url: string) => {
    return { success: true };
};
